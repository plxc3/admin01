server:
  port: 1000
spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: "jdbc:mysql://121.89.177.244:3306/csdn?useUnicode=true&characterEncoding=UTF-8"
      password: "22446688"
      username: "root"
  profiles:
    active: pro

#mybatis-plus配置控制台打印完整带参数SQL语句
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)